services:
  db-tests:
    container_name: db-tests
    build:
      dockerfile: docker/Dockerfile-db-test
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=iselPs123
    ports:
      - "5432:5432"
